# ===================================
# Configuración de Entorno - TaskFlow
# ===================================
# Copia este archivo a .env y completa los valores
# IMPORTANTE: ¡Nunca compartas el archivo .env real!

# ===== Configuración Básica =====
NODE_ENV=development  # development, test, production
PORT=3000             # Puerto del servidor
LOG_LEVEL=info        # Nivel de logs: error, warn, info, debug

# ===== Base de Datos =====
# Formato: postgresql://usuario:contraseña@host:puerto/nombre_bd?schema=public
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/taskflow?schema=public"

# ===== Autenticación JWT =====
# Genera un secreto seguro con: openssl rand -hex 32
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRES_IN=7d     # Duración del token (ej: 15m, 1h, 7d)

# ===== Límite de Tasas (Rate Limiting) =====
RATE_LIMIT_WINDOW_MS=900000  # Ventana de tiempo en ms (15 minutos)
RATE_LIMIT_MAX=1000          # Máximo de peticiones por ventana

# ===== CORS (Seguridad) =====
# URLs permitidas, separadas por comas
FRONTEND_URL=http://localhost:5173

# ===== Configuración de Correo (Opcional) =====
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_SECURE=false  # true para puerto 465, false para otros
# SMTP_USER=usuario
# SMTP_PASS=contraseña
# EMAIL_FROM=noreply@taskflow.app

# ===== Variables Específicas de Entorno =====
# Agrega aquí cualquier otra variable específica de tu entorno
